<Window x:Class="ConnectionString.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConnectionString"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="TxtResultaat" Margin="10px"/>
        <DataGrid Grid.Row="0" Grid.Column="1" Margin="20px" Name="dgGrid"/>
        <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Center">
            <Button x:Name="buttonX" Click="buttonX_Click" Height="20px" Width="150px" Content="Connect" Margin="10px" />
            <Button x:Name="buttonY" Click="buttonY_Click" Height="20px" Width="150px" Content="Haal Data" Margin="10px" />
            <Button x:Name="buttonZ" Click="buttonZ_Click" Height="20px" Width="150px" Content="Haal 1 stuk data" Margin="10px" />

        </StackPanel  >

    </Grid>
</Window>
